@Library('jenkins-ext') _

require 'helm'
initialize this

kubepipe(serviceAccount: 'jenkins-admin') {
    helm 'repo add stable https://kubernetes-charts.storage.googleapis.com/'
    helm 'repo update'
    helm 'install sonarqube stable/sonarqube -n demo'
}